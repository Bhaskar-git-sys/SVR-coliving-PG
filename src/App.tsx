import {useState,useEffect,useCallback} from 'react';import AuthPage from '@/components/AuthPage';import DashboardLayout from '@/components/DashboardLayout';import RoomChart from '@/components/RoomChart';import ResidentsPage from '@/components/ResidentsPage';import PaymentsPage from '@/components/PaymentsPage';import SettingsPage from '@/components/SettingsPage';import OwnerProfilePage from '@/components/OwnerProfilePage';import RevenuePage from '@/components/RevenuePage';import type {Page,OwnerAccount,PGData,AppUser} from '@/types';import {getOwnerAccount,getOwnerSession,clearOwnerSession,getPGData,savePGData} from '@/lib/storage';import {clearAuth,getCurrentUser} from '@/lib/auth';
const fallback:OwnerAccount={name:'SVR Owner',email:'',mobile:'',password:'',pgName:'SVR Co-Living PG',pgAddress:''};export default function App(){const[user,setUser]=useState<AppUser|null>(null);const[owner,setOwner]=useState<OwnerAccount|null>(null);const[page,setPage]=useState<Page>('rooms');const[data,setData]=useState<PGData|null>(null);const[booted,setBooted]=useState(false);useEffect(()=>{const u=getCurrentUser();const d=getPGData();if(u){setUser(u);setOwner(getOwnerAccount()||fallback)}setData(d);setBooted(true)},[]);const login=useCallback(()=>{const u=getCurrentUser();if(u){setUser(u);setOwner(getOwnerAccount()||fallback);setPage('rooms')}},[]);const logout=useCallback(()=>{clearAuth();clearOwnerSession();setUser(null);setOwner(null);setPage('rooms')},[]);const update=useCallback((d:PGData)=>{savePGData(d);setData(d)},[]);if(!booted)return <div className="min-h-screen bg-navy-900 flex items-center justify-center"/>;if(!user||!owner||!data)return <AuthPage onLogin={login}/>;const restricted=user.role!=='ADMIN'&&['payments','revenue','settings','profile'].includes(page);if(restricted){if(page!=='rooms')setPage('rooms');return <DashboardLayout page="rooms" setPage={setPage} owner={owner} user={user} onLogout={logout}><div className="glass-dark rounded-2xl p-10 text-center"><h1 className="font-display text-2xl font-bold">Access Denied</h1><p className="text-navy-300 mt-2">This area is available to administrators only.</p></div></DashboardLayout>}return <DashboardLayout page={page} setPage={setPage} owner={owner} user={user} onLogout={logout}>{page==='rooms'&&<RoomChart data={data} setData={update}/>} {page==='residents'&&<ResidentsPage data={data} setData={update}/>} {page==='payments'&&<PaymentsPage data={data} setData={update}/>} {page==='revenue'&&<RevenuePage data={data}/>} {page==='settings'&&<SettingsPage data={data} setData={update} onOwnerReset={logout}/>} {page==='profile'&&<OwnerProfilePage owner={owner} setOwner={setOwner}/>}</DashboardLayout>}
